1. У нас есть базоваое приложение usummit-ios и usummit-ios-clones. clones - это по факту конфигурации(кастомизации) базового, но с разными лого, картинками и пр.
2.![[Pasted image 20240220145401.png]]

Главное сейчас это Jenkinsfile_common - там вызываются все функции из Fastfile

Приколы - 

```
if (fileExists(

"${confDir}/${params.CloneName}/appstore_key_config.plist"

)) {

println("\033[31mUsing AppStore Connect ApiKey\033[0m")

creds = creds + file(

credentialsId: "appstore-${params.CloneName}-apikey",

variable: "APPSTORE_APIKEY_FILEPATH",

)

}

```

![[Pasted image 20240220150255.png]]